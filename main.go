package main

import (
)

// Custom Firebase credential file name
// firestore-meilisearch-sync <collection name> -r -f firebase.json

// Rebuild index:
// firestore-meilisearch-sync <collection name> -r

// Listen to changes:
// firestore-meilisearch-sync <collection name> -l

// Rebuild index & listen to changes:
// firestore-meilisearch-sync <collection name> -r -l
func main() {
	// Read the firebase credentials
}
